name: Publish Devcontainer to GHCR

on:
  push:
    branches:
      - develop
    paths:
      - ".devcontainer/Dockerfile"
      - ".github/workflows/publish-devcontainer.yml"
      - ".python-version"
    tags:
      - "v*"

  workflow_dispatch:

env:
  IMAGE_NAME: ${{ github.repository }}/devcontainer
  REGISTRY: ghcr.io

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Container Registry
        uses: docker/login-action@v4
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}

      - name: Extract Metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}
          labels: |
            maintainer=Ethan Bell (emanb29) <eb@ethanbell.me>
            org.opencontainers.image.authors=Ethan Bell (emanb29) <eb@ethanbell.me>
            org.opencontainers.image.description=Dev Container for Python development with uv-based dependency management and environment configuration.
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.title=uv-base-project
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.vendor=Ethan Bell (emanb29) <eb@ethanbell.me>

      - name: Build and Push Devcontainer Image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: .devcontainer/Dockerfile
          push: true
          provenance: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
